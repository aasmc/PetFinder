<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_onboarding">

    <include app:graph="@navigation/nav_animalsnearyou" />
    <include app:graph="@navigation/nav_search" />
    <include app:graph="@navigation/nav_onboarding" />
    <include-dynamic
        android:id="@id/dynamicFeatureSharing"
        app:graphPackage="ru.aasmc.petfinder.sharing"
        app:graphResName="nav_sharing"
        app:moduleName="sharing" />
    <fragment
        android:id="@+id/report"
        android:name="ru.aasmc.petfinder.report.ReportDetailFragment"
        android:label="@string/report_a_concern"
        tools:layout="@layout/fragment_report_detail" />

</navigation>